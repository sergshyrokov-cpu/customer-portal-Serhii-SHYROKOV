# Execution state of the active story-delivery workflow.
# Schema: docs/workflow/state-schema.md
# Writer: story-orchestrator ONLY. Stage Skills never write this file.
version: 1
story: US-001
workflow: story-delivery
current_stage: ARCHIVED
previous_stage: HUMAN_PR_APPROVAL
last_completed_stage: RECONCILIATION
status: ARCHIVED
attempt: 1
last_invoked_skill: reconciliation-reviewer
last_result:
  verdict: PASS
  stage: RECONCILIATION
  recorded_at: 2026-08-31T12:57:34Z
last_artifacts:
  - type: reconciliation
    path: docs/reviews/reconciliation/US-001-reconciliation.md
    version: 1
  - type: traceability
    path: docs/reconciliation/US-001-traceability.md
    version: 1
pending_human_gate: null
blocking_issues: []
non_blocking_findings:
  - "MANUAL ARCHIVE (2026-09-03, superseded by .NET port): the HUMAN_PR_APPROVAL gate opened 2026-08-31T12:57:34Z was never decided (stayed PENDING) and this workflow instance never progressed through PR_PREPARATION / READY_FOR_PR / COMPLETED — no Java-specific Pull Request was ever created or merged for this Spring Boot implementation of US-001. Actual delivery of US-001, US-002, and US-003 was completed via a direct ASP.NET Core / EF Core / C# port (dotnet/CustomerPortal), merged to main as PR #2 (US-001), #1 (US-002), #3 (US-003) on sergshyrokov-cpu/customer-portal-Serhii-SHYROKOV. This Java implementation remains in the repository (src/main/java) as the original reference implementation the port was based on; it is not itself the delivered artifact. Archived directly from HUMAN_PR_APPROVAL by explicit human instruction, bypassing the normal COMPLETED transition since that stage's meaning (\"a human confirms the Pull Request has been merged\") does not apply here."
  - "RECONCILIATION PASS (0 Critical, 0 Major, 5 Minor, 2 Informational): AC-001..AC-007 all RECONCILED in docs/reconciliation/US-001-traceability.md. Artifact chain intact (all v1, none SUPERSEDED, both human gates recorded). Currency verified: git diff fb833b0..HEAD -- src/ is EMPTY, so implementation_verification v1 and security_review v1 remain current. PR candidate scope: 50 include / 12 exclude; no secret, no generated DB file, no unrelated source change in the include set."
  - "RC-1 (Minor, = SEC F-1 / IMPL_VERIFICATION MF-5): RegistrationRequest record toString() renders the plaintext password. No reachable leak path today (zero logger/System.out/println in src/main; GlobalExceptionHandler never touches the bound DTO; no debug/SQL logging config). ACCEPTED-WITH-RECOMMENDATION; follow-up: custom masking toString() consistent with Customer.toString()."
  - "RC-2 (Minor, = SEC F-2 / IMPL_VERIFICATION MF-2 / IMPL D-4): implemented 400 (one combined message) and 415 error message strings differ verbatim from the OpenAPI illustrative examples; 409 matches exactly, AC-6 body shape preserved. Implemented messages more generic - no extra disclosure. Optional openapi-designer example alignment."
  - "RC-3 (Minor, = SEC F-3 / IMPL_VERIFICATION MF-3): CustomerService.register check-then-act (existsByEmail then save); concurrent duplicate trips uq_customer_email as unmapped DataIntegrityViolationException -> 500 not 409. Constraint still prevents the duplicate row; no data-integrity/enumeration impact beyond accepted OD-003:A. No AC/design/test covers concurrency (OD-005:A out of scope). Follow-up Story to map to 409."
  - "RC-4 (Minor, = spec-review F-1 / IMPL_VERIFICATION MF-4 / SEC F-4 / plan-review F-1): docs/decisions/US-001-open-decisions.md v1 still shows OD-001..OD-006 OPEN; authoritative resolutions in history.jsonl at HUMAN_SPEC_APPROVAL, applied consistently, human ruled non-blocking at HUMAN_PLAN_APPROVAL. Owned by us-clarifier (publish v2). Not a RECONCILIATION loop-back target."
  - "RC-5 (Informational, = SEC F-5): default-profile datasource AUTO_SERVER=TRUE with sa / blank password - H2 mixed-mode TCP listener, no credential. APPROVED config (DB design line 172, plan M-2, impact analysis); local-dev / training data only. Future hardening Story drops AUTO_SERVER or binds loopback + externalizes an H2 password."
  - "RC-6 (Informational, = SEC F-6): spring.datasource.password: \"\" + username sa committed in application.yaml. Empty string is the H2 default (not a secret), SC-7 not breached; externalize when a real datasource is introduced."
  - "RC-7 (Informational, = IMPL D-5): schema.sql id column 'GENERATED BY DEFAULT AS IDENTITY' with no explicit NOT NULL (H2 2.x implicit). ddl-auto=validate passes on both profiles; CustomerSchemaTest.idColumnIsBigintNotNull confirms BIGINT + not-nullable."
  - "RC-8 (Minor, scope/hygiene): .gitignore (+ /docs/harness-consistency-review.md) and docs/evidence/.gitignore (+4 harness-* entries) committed in c72ace3 ignore pre-existing harness scaffolding, not US-001 output. No code/secret/runtime-artifact impact. .gitignore also carries M-3 (/data/) so it stays in the PR. Flagged for the human diff reviewer at HUMAN_PR_APPROVAL."
  - "Accepted deviations re-confirmed: D-1 exception/InvalidPasswordException (FR-6 service re-check; plan C-12 left the type unspecified; mapped to 400, not reachable from HTTP flow); D-2 CustomerPersistenceTest uses @SpringBootTest not @DataJpaTest (all C-T2 scenarios covered); MF-1 the only change to a test-writer-owned test is a benign 2-char BCrypt-literal fix in CustomerSchemaTest, no assertion/scenario/outcome changed."
  - "--- prior SECURITY_REVIEW findings retained below ---"
  - "SECURITY_REVIEW PASS (0 Critical, 0 Major, 4 Minor, 2 Informational): SEC-1..SEC-11 all independently verified in code/config/tests; security tests exercise the real SecurityFilterChain. Security-sensitive Open Decisions (OD-002 CSRF:B, OD-003 enumeration:A, OD-005 anti-abuse:A) resolved by human at HUMAN_SPEC_APPROVAL 2026-08-31T07:48:48Z and correctly implemented. runtime_checks PARTIAL, semantic_analysis TEXT_FALLBACK, build/tests reused from implementation_verification v1, NO dependency vulnerability scanning."
  - "SEC F-1 (Minor, PASSWORD_HANDLING; RECONCILIATION to record accepted-with-recommendation): RegistrationRequest is a record - generated toString() renders the plaintext password. No reachable leak path today. SEC-3 holds. Follow-up: custom masking toString() consistent with Customer.toString()."
  - "SEC F-2 (Minor, API_SECURITY/doc): implemented 400/415 error message strings differ verbatim from OpenAPI illustrative examples (409 matches exactly; AC-6 shape preserved). Implemented messages are more generic - no extra disclosure. Same as IMPL_VERIFICATION MF-2 / D-4. Optional openapi-designer example alignment."
  - "SEC F-3 (Minor, PERSISTENCE/robustness): CustomerService.register check-then-act (existsByEmail then save); concurrent duplicate trips uq_customer_email as unmapped DataIntegrityViolationException -> 500 not 409. Constraint still prevents the duplicate row; no data-integrity/enumeration impact beyond accepted OD-003:A. No AC/design/test covers concurrency (OD-005:A out of scope). Follow-up Story to map to 409. Same as IMPL_VERIFICATION MF-3."
  - "SEC F-4 (Minor, doc; carried F-1 / MF-4): docs/decisions/US-001-open-decisions.md v1 still shows OD-001..OD-006 OPEN; resolutions authoritative in history.jsonl and applied. Owned by us-clarifier (publish v2). No implementation impact."
  - "SEC F-5 (Informational, CONFIGURATION): default-profile datasource uses AUTO_SERVER=TRUE with sa / blank password - H2 mixed-mode TCP listener, no credential. APPROVED config (DB design §8 line 172, plan M-2, impact analysis); local-dev / training data only; test profile isolated in-memory. Recommendation: future hardening Story drops AUTO_SERVER or binds loopback + externalizes an H2 password."
  - "SEC F-6 (Informational, SECRET_MANAGEMENT): spring.datasource.password: \"\" + username sa committed in application.yaml. Empty string is the H2 default (not a secret), SC-7 not breached; externalize when a real datasource is introduced."
  - "--- prior IMPLEMENTATION_VERIFICATION findings retained below ---"
  - "IMPL_VERIFICATION PASS: ./gradlew clean build independently reproduced — BUILD SUCCESSFUL (exit 0); 62 tests, 0 failures, 0 errors, 0 skipped across 7 suites. AC-001..AC-007 all VERIFIED with executable test evidence. 0 Critical, 0 Major, 5 Minor findings."
  - "MF-1: implementer corrected a malformed 61-char dummy BCrypt literal to 60 chars (2 occurrences) in test-writer-owned CustomerSchemaTest.caseInsensitiveDuplicateEmailCollidesOnTheUniqueConstraint (VARCHAR(60) per PC-9). git diff f6e7eae..fb833b0 confirms this is the ONLY change to a test-writer test; no assertion/scenario/outcome changed; pre-fix the test was a false RED (first INSERT errored before the constraint assertion). Was IMPLEMENTATION D-3. test-writer owns the suite; RECONCILIATION to note the ownership crossing."
  - "MF-2: implemented 400 (one combined missing/malformed/unknown-field message) and 415 error message strings differ verbatim from the OpenAPI illustrative examples; the 409 message matches exactly, AC-6 body shape preserved, tests assert status+shape only. Consistent with IMPLEMENTATION D-4 / plan-review F-4. Optional: openapi-designer aligns examples in a future revision."
  - "MF-3 (SECURITY_REVIEW / RECONCILIATION to weigh): CustomerService.register uses check-then-act (existsByEmail then save); a concurrent duplicate would trip uq_customer_email as DataIntegrityViolationException, which is unmapped and falls through to 500 rather than 409. No AC/test/design covers concurrency; anti-abuse out of scope (OD-005). Latent robustness gap, not a requirements defect; candidate for a follow-up Story."
  - "MF-4 (carried F-1): docs/decisions/US-001-open-decisions.md v1 still shows OD-001..OD-006 OPEN; authoritative resolutions (OD-001:A, OD-002:B, OD-003:A, OD-004:A, OD-005:A, OD-006:A) are in history.jsonl at HUMAN_SPEC_APPROVAL and applied correctly. Owned by us-clarifier (publish v2). No implementation impact."
  - "MF-5 (SECURITY_REVIEW to weigh): RegistrationRequest is a record, so its generated toString() includes the plaintext password component (Customer.toString() was hand-written to exclude passwordHash). No reachable leak path found — production code has NO logger / System.out / println (grep over src/main = no matches); GlobalExceptionHandler never touches the bound DTO. SEC-3 ('never logged') holds today; a defensive custom toString() is a SECURITY_REVIEW call."
  - "R-1 CLOSED (verified at IMPL_VERIFICATION): OffsetDateTime <-> TIMESTAMP WITH TIME ZONE validates under ddl-auto=validate on H2 2.x / Hibernate ORM 7.4.5; context boots clean on the test profile against the hand-written schema.sql."
  - "R-2 (SECURITY_REVIEW owns): security/SecurityConfig scopes BOTH the public matcher and the CSRF exemption to POST /api/v1/customers via one RequestMatcher; anyRequest().authenticated(); formLogin/httpBasic/logout disabled; HttpStatusEntryPoint(401). RegistrationSecurityPostureTest 4/4 green. 'CSRF still enforced elsewhere' stays unassertable in US-001 (no second writable endpoint)."
  - "R-3 CLOSED (verified): password 12..72 bound measured in UTF-8 BYTES in both PasswordPolicyValidator and the service re-check; PasswordPolicyValidatorTest + CustomerRegistrationApiTest cover 11/12/72/73-byte and the multi-byte 73-byte vector."
  - "R-6 CLOSED (verified): config/JpaAuditingConfig DateTimeProvider returns OffsetDateTime.now(ZoneOffset.UTC); CustomerPersistenceTest asserts the UTC offset on created_at and updated_at and created_at immutability."
  - "R-7 (SECURITY_REVIEW owns): @ValidPassword message is the fixed string 'Password does not meet the security policy.'; no-echo asserted by PasswordPolicyValidatorTest.defaultMessageNeverEchoesASubmittedValue and CustomerRegistrationApiTest.passwordValidationMessageDoesNotEchoTheSubmittedValue."
  - "R-8 (RECONCILIATION owns): scope held — no GET /api/v1/customers/{id}, no login/UserDetailsService, no rate limiting. Change set = 16 new production files + InvalidPasswordException + 3 test files + M-1..M-4; the only change outside that is the benign CustomerSchemaTest fixture fix (MF-1). git confirms no unrelated changes."
  - "D-1 accepted at IMPL_VERIFICATION: exception/InvalidPasswordException is a justified required supporting change (FR-6 service re-check; plan C-12 left the type unspecified); domain exception in the exception package (AD-6), mapped to 400, not reachable from the HTTP flow."
  - "D-2 accepted at IMPL_VERIFICATION: CustomerPersistenceTest uses @SpringBootTest instead of the indicative @DataJpaTest skeleton; all three C-T2 scenarios covered and passing."
  - "D-5 (carried): schema.sql id column uses 'GENERATED BY DEFAULT AS IDENTITY' with no explicit NOT NULL (H2 2.x implicit); ddl-auto=validate passes on both profiles; CustomerSchemaTest.idColumnIsBigintNotNull confirms BIGINT + not-nullable."
  - "Test-isolation smell (not a finding): CustomerSchemaTest and CustomerPersistenceTest share one JVM-lifetime in-memory H2 (DB_CLOSE_DELAY=-1); CustomerSchemaTest inserts two non-transactional rows that outlive the test. Harmless because all other tests use randomized emails."
  - "ORCHESTRATOR STATE REPAIR (TEST_WRITING continue, 2026-08-31): docs/plans/US-001-implementation-plan.md front-matter status was still DRAFT after PLAN_REVIEW (PASS) and HUMAN_PLAN_APPROVAL (2026-08-31T11:20:00Z). Corrected to status: APPROVED, updated_at 2026-08-31T11:20:00Z. No version bump, no content change. (Retained for audit.)"
started_at: 2026-08-30T23:50:01Z
updated_at: 2026-09-03T13:05:22Z
completed_at: null
archived_at: 2026-09-03T13:05:22Z
